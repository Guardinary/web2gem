# Worker secrets used by the Cloudflare Deploy Button.
# Non-secret Worker configuration is visible in wrangler.jsonc vars.
API_KEYS=

# Account-pool admin auth. Use a comma-separated list. Empty leaves admin auth
# unconfigured; placeholder keys such as changeme are rejected.
ADMIN_KEYS=
