# Worker secrets used by the Cloudflare Deploy Button.
# Non-secret Worker configuration is visible in wrangler.jsonc vars.
API_KEYS=

# Account-pool admin auth. Admin endpoints fail closed when this is empty or
# set to placeholders such as changeme.
ADMIN_KEYS=
