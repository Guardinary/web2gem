# Required for the Gemini account admin UI and admin API.
# Enter one or more admin keys as a comma-separated list.
ADMIN_KEYS=

# Optional public API auth. Leave empty to disable public API authentication.
API_KEYS=
